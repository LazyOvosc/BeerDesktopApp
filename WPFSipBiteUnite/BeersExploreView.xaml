<UserControl x:Class="WPFSipBiteUnite.BeersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WPFSipBiteUnite"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="572">
    <Grid>
        <!-- StackPanel for organization -->
        <StackPanel>
            <TextBlock Text="Explore beers"
                       FontSize="50"
                       Foreground="{StaticResource Neutral300Brush}"/>

            <!-- ItemsControl to display beers -->
            <ItemsControl ItemsSource="{Binding Beers}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <!-- Customize the appearance of each beer item -->
                        <Border BorderBrush="White" BorderThickness="1" Margin="5">
                            <!-- Assuming Beer has a property named BeerName -->
                            <TextBlock Text="{Binding BeerType}" FontSize="16" Foreground="White" />
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Grid>
</UserControl>
